apiVersion: v1
kind: ServiceAccount
metadata:
  name: csv-to-yaml-sa
  namespace: default
  labels:
    app: csv-to-yaml
    app.kubernetes.io/name: csv-to-yaml
    app.kubernetes.io/component: service-account

---
# Optional: Create a ClusterRole if your application needs cluster-wide permissions
# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRole
# metadata:
#   name: csv-to-yaml-role
#   labels:
#     app: csv-to-yaml
#     app.kubernetes.io/name: csv-to-yaml
# rules:
# - apiGroups: [""]
#   resources: ["pods", "services", "endpoints", "persistentvolumeclaims", "secrets", "configmaps"]
#   verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
# - apiGroups: ["apps"]
#   resources: ["deployments", "replicasets", "statefulsets"]
#   verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

---
# Optional: Bind the ClusterRole to the ServiceAccount
# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRoleBinding
# metadata:
#   name: csv-to-yaml-role-binding
#   labels:
#     app: csv-to-yaml
#     app.kubernetes.io/name: csv-to-yaml
# subjects:
# - kind: ServiceAccount
#   name: csv-to-yaml-sa
#   namespace: default
# roleRef:
#   kind: ClusterRole
#   name: csv-to-yaml-role
#   apiGroup: rbac.authorization.k8s.io
