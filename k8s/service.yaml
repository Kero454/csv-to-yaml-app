apiVersion: v1
kind: Service
metadata:
  name: csv-to-yaml-service
  namespace: csv-to-yaml
spec:
  type: ClusterIP
  selector:
    app: csv-to-yaml
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
