apiVersion: v1
kind: Service
metadata:
  name: {{ include "csv-to-yaml.fullname" . }}
  labels:
    {{- include "csv-to-yaml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "csv-to-yaml.selectorLabels" . | nindent 4 }}
